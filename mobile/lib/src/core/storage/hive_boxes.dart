/// Hive box name constants used across the app.
/// Box initialization is handled by [OfflineQueueService.init()].
class HiveBoxes {
  /// Box storing serialized offline queue entries as JSON strings.
  static const String offlineQueue = 'offline_queue_box';
}
