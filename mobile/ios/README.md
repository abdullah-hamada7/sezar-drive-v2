# iOS scaffold

Run from the `mobile/` directory when Flutter is installed:

```bash
flutter create --platforms=ios .
```

Then open `ios/Runner.xcworkspace` in Xcode to configure signing, permissions (camera, location, notifications), and deploy.
