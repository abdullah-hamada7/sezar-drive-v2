# Fleet Management Platform — Production Environment Configuration
# This file serves as a template for the .env file required on the production server.

# ─── Project Identity ─────────────────────────────
PROJECT_NAME=sezar-drive
aws_region=us-east-1

# ─── Domain & SSL ────────────────────────────────
# Used by Caddy for automatic HTTPS and CORS
DOMAIN_NAME=your-domain.com
EMAIL_FOR_SSL=your-email@example.com

# ─── Database Identity ───────────────────────────
# Note: These MUST MATCH the secrets in AWS Secrets Manager
POSTGRES_DB=sezar_drive
POSTGRES_USER=postgres
POSTGRES_PASSWORD=REAL_PASSWORD_FROM_SECRETS_MANAGER

# ─── Backend Specifics ───────────────────────────
# Used for internal health checks and routing
PORT=3000
NODE_ENV=production
FRONTEND_URL=https://your-domain.com

# ─── Storage ─────────────────────────────────────
# Note: Bucket name is generated by Terraform (see outputs)
S3_REGION=us-east-1
S3_BUCKET=sezar-drive-photos-XXXXX
